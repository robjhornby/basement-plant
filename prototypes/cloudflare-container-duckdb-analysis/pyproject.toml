[project]
name = "cloudflare-container-duckdb-analysis-prototype"
version = "0.1.0"
description = "Throwaway Cloudflare Container DuckDB analysis prototype."
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.40.0",
    "duckdb>=1.5.4",
]

